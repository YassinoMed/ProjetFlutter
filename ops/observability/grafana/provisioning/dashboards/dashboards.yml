apiVersion: 1

providers:
  - name: MediConnect
    orgId: 1
    folder: MediConnect
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
